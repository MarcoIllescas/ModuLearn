services:
  triton:
    image: nvcr.io/nvidia/tritonserver:26.08-py3

    ports: 
      - "8000:8000"
      - "8001:8001"
      - "8002:8002"

    volumes:
      - ./serving/model_repository:/models

    command:
      - tritonserver
      - --model-repository=/models
      # - --model-control-mode=poll
      # - --model-control-mode=explicit
      # - --repository-poll-secs=15